[versions]
# Gradle 및 Kotlin 플러그인
agp = "8.13.0"
kotlin = "2.0.21"

# AndroidX 및 Jetpack
coreKtx = "1.17.0"
lifecycle = "2.9.4"             # 라이프사이클 관련 버전을 하나로 통일 (runtime, viewmodel)
activityCompose = "1.11.0"
composeBom = "2025.10.01"
appcompat = "1.6.1"
recyclerview = "1.3.2"

# 데이터/네트워킹
kotlinCoroutines = "1.10.2"      # kotlinx-coroutines-android 버전 통일
room = "2.8.3"                  # Room 최신 안정화 버전 (이전 2.8.3은 높은 버전이라 2.6.1로 변경)
retrofit = "3.0.0"              # Retrofit 최신 안정화 버전 (이전 3.0.0은 아직 정식 릴리즈가 아닐 가능성 높음)
okhttp = "5.3.0"               # OkHttp 버전 별도 정의 (인터셉터용)

# KSP 컴파일러
ksp = "2.0.21-1.0.25"         # Kotlin 버전에 맞는 KSP 버전

# 테스트
junit = "4.13.2"
junitVersion = "1.3.0"
espressoCore = "3.7.0"
# coroutines = "1.3"             # (삭제) kotlinx-coroutines-android 항목을 사용하므로 중복 제거

[libraries]
# ----------------------------------------------------------------------
# 1. AndroidX 기본 및 Compose
# ----------------------------------------------------------------------
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
# 👇 Unresolved reference 오류 해결: viewmodel ktx 추가
androidx-lifecycle-viewmodel-ktx = { module = "androidx.lifecycle:lifecycle-viewmodel-ktx", version.ref = "lifecycle" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-material = { group = "androidx.compose.material", name = "material"}
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-compose-material3-adaptive-navigation-suite = { group = "androidx.compose.material3", name = "material3-adaptive-navigation-suite" }
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }
androidx-recyclerview = { group = "androidx.recyclerview", name = "recyclerview", version.ref = "recyclerview" }

# ----------------------------------------------------------------------
# 2. 데이터 및 네트워킹 (GPT, Room DB)
# ----------------------------------------------------------------------
kotlinx-coroutines-android = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "kotlinCoroutines" }
# 👇 Room 관련 의존성 추가
androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "room" }
androidx-room-ktx = { module = "androidx.room:room-ktx", version.ref = "room" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "room" } # KSP로 사용될 컴파일러

# 👇 Retrofit 관련 의존성 추가
retrofit = { module = "com.squareup.retrofit2:retrofit", version.ref = "retrofit" }
converter-gson = { module = "com.squareup.retrofit2:converter-gson", version.ref = "retrofit" }
okhttp3-logging-interceptor = { module = "com.squareup.okhttp3:logging-interceptor", version.ref = "okhttp" }

# ----------------------------------------------------------------------
# 3. 테스트
# ----------------------------------------------------------------------
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }


[plugins]
# ----------------------------------------------------------------------
# 4. 플러그인 정의 (ksp 포함)
# ----------------------------------------------------------------------
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
# 👇 KSP 플러그인 정의 추가
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }